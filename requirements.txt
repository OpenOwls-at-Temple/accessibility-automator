# ── Accessibility Automator — backend + engine dependencies ──
# Python 3.11+ required (see ai_specs/conventions.md)

# Document processing
python-pptx>=0.6.23          # PPTX read/edit/write
pikepdf>=8                   # PDF structure (Phase 1 PDF path)
pdf2image>=1.17              # rasterize PDF pages for OCR
pytesseract>=0.3.10          # OCR scanned PDFs (requires Tesseract on the OS)
Pillow>=10                   # image resize before sending to the LLM

# Config + HTTP
PyYAML>=6                    # config.yaml loading
httpx>=0.27                  # OpenAI-compatible LLM client + API tests

# Web layer (FastAPI) — used once the backend app is built
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2
python-multipart>=0.0.9     # file uploads

# Dev / test
pytest>=8
pytest-asyncio>=0.23
black>=24
ruff>=0.5
reportlab>=4                  # builds PDF test fixtures in remediator/tests/conftest.py
