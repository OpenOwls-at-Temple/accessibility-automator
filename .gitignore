# macOS
.DS_Store

# Secrets / env
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Node / frontend
node_modules/
dist/

# Per-user workspace storage (runtime data)
storage/
data/

# Local test decks — never commit real faculty/student content (copyright/FERPA).
# The folder + README are tracked so the convention is discoverable; the files are not.
testdata/*
!testdata/README.md

# Local user-allowlist database (SQLite)
*.db
